import { StandardLayoutDirective } from './standard-layout.directive';

describe('StandardLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new StandardLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
